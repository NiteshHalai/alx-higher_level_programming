#!/usr/bin/python3
def nqueens(n):
    """My addition function
    Args:
        a: first integer
        b: second integer
    Returns:
        The return value. a + b
    """
    argv = open('/proc/self/cmdline').read().split('\0')
    print(argv)
    print(n)
    return (n)
